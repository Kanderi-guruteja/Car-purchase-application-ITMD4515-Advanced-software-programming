<?xml version="1.0" encoding="UTF-8"?>
<web-app version="4.0" xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd">

    <!-- JSF Configuration -->
    <context-param>
        <param-name>javax.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>

    <!-- Welcome File -->
    <welcome-file-list>
        <welcome-file>login.xhtml</welcome-file>
    </welcome-file-list>

    <!-- Security Constraints -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Admin Section</web-resource-name>
            <url-pattern>/admin/*</url-pattern>
            <url-pattern>/admin/welcome.xhtml</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>ADMIN_ROLE</role-name>
        </auth-constraint>
    </security-constraint>

    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Manufacturer Section</web-resource-name>
            <url-pattern>/manufacturer/*</url-pattern>
            <url-pattern>/manufacturer/welcome.xhtml</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>MANUFACTURER_ROLE</role-name>
        </auth-constraint>
    </security-constraint>

    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Sales Staff Section</web-resource-name>
            <url-pattern>/salesstaff/*</url-pattern>
            <url-pattern>/salesstaff/welcome.xhtml</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>SALESSTAFF_ROLE</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Security Roles -->
    <security-role>
        <role-name>ADMIN_ROLE</role-name>
    </security-role>

    <security-role>
        <role-name>MANUFACTURER_ROLE</role-name>
    </security-role>

    <security-role>
        <role-name>SALESSTAFF_ROLE</role-name>
    </security-role>

    <!-- Login Configuration -->
    <login-config>
        <auth-method>FORM</auth-method>
        <form-login-config>
            <form-login-page>/login.xhtml</form-login-page>
            <form-error-page>/error.xhtml</form-error-page>
        </form-login-config>
    </login-config>

</web-app>
